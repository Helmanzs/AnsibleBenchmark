---
- name: Stop target LXD Instance(s)
  hosts: "{{ target }}"
  gather_facts: false
  tasks:
    - name: Stop Target LXD Instance(s)
      ansible.builtin.include_role:
        name: common-stop
      vars:
        vm_name: "{{ inventory_hostname }}"
